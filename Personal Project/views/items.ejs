<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= item.Name %> </title>
    
</head>
<body>
    <h1><%= item.Name  %> </h1>
    <ul>
        <li><%= item.DamagePhys  %> </li>
        <li><%= item.Description  %> </li>
        <li><%= item.Icon.slice(3) %> </li>
        <img src="" alt="" srcset="https://garlandtools.org/files/icons/item/30446.png">
        <img src="../src/ui/icon/<%=item.Icon.slice(3)%>" alt="" srcset="">
        <% for(let [key,value] of Object.entries(item)) {%> 
        <li>
                <%= key  %>: <%= value  %>
            
           <%  if(value == "[object Object]"){%>
           
            <% for(let d of Object.values(value)){%>

                 
                   <%= JSON.stringify(d)  %>   
                   <% }%>
                   <% }%>
    
        </li>
        <% }  %> 

    </ul>
  
    
</body>
</html>

